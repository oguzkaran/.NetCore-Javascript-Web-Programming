import {isPrime} from '../../csd-modules/csdnumberutil.mjs';
import chai from 'chai';

const assert = chai.assert

describe("Is Prime test", () => {
    describe("Is Prime test", () => {
        it("isPrime should return true", () => assert.equal(isPrime(19), true))
    })
})
